package com.electronwill.utils

import org.tuubes.core.worlds.LocalWorld

final class Location(val coords: Vec3d, val world: LocalWorld) {
  //TODO withX, withY, withZ, withWorld, implicits and more
}
