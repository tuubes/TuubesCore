description "Open source Photon project: let's create a better server!"

subprojects {
	apply plugin: 'scala'
	repositories {
		mavenCentral()
		maven { url 'https://jitpack.io' }
	}
	dependencies {
		compile 'org.scala-lang:scala-library:2.12.4'
	}
}